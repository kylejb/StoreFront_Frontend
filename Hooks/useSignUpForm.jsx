import React, { useState } from 'react';

// experimenting with custom hooks to help with forms
const useSignUpForm = (callbackFn) => {
    const [inputs, setInputs] = useState({});

    return null;
}

export default useSignUpForm;